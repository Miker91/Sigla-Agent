# Disclaimer: This function was generated by AI. Please review before using.
# Agent Name: data_wrangling_agent
# Time Created: 2025-04-26 17:13:15

def data_wrangler(data_list):
    import pandas as pd
    import numpy as np
    '''
    Wrangle the data provided in data_list by removing the first row from the dataframe.

    data_list: A list of one or more pandas data frames containing the raw data to be wrangled.
    '''


    # Ensure input is a list, if not convert it to list
    if not isinstance(data_list, list):
        data_list = [data_list]
    
    # Based on the prompt, there is a single dataframe provided as "main".
    # We only need to remove the first row from this dataframe.
    df = data_list[0].copy()
    
    # Remove the first row (index 0) from the dataframe using iloc to slice rows from second row onwards
    df_wrangled = df.iloc[1:].reset_index(drop=True)
    
    # Return the resulting dataframe
    return df_wrangled